---
phase: 06-cuda-wiring-llvm-lowering-frontend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/triton/experimental/gluon/language/_core.py
  - python/triton/experimental/gluon/language/_semantic.py
autonomous: true
requirements:
  - SHAPI-01

must_haves:
  truths:
    - "gl.call() accepts shared_memory_descriptor arguments alongside tensors — no TypeError from isinstance check or to_tensor"
    - "PaddedSharedLayout descriptors are rejected at the frontend with a NotImplementedError matching the f64 guard style"
    - "arg_params for shared args carry memory_space: \"shared\" key, absent for distributed args"
  artifacts:
    - python/triton/experimental/gluon/language/_core.py  (modified: gl.call arg dispatching)
    - python/triton/experimental/gluon/language/_semantic.py  (modified: isinstance, PaddedSharedLayout guard, arg_params)
  key_links:
    - "_core.py:803 arg-dispatch loop → _semantic.py:253 isinstance — must agree on accepted types"
    - "_semantic.py arg_params memory_space key → compiler.py infer_result branch (Plan 06-02)"
---

<objective>
Enable `gl.call()` to accept `shared_memory_descriptor` arguments alongside tensors, with proper frontend validation and backend-signaling via `arg_params`.

Purpose: The Gluon frontend must forward shared-memory descriptors to the inference hook without raising `TypeError`, and signal to the backend which args are shared via a `memory_space` discriminator in `arg_params`.
Output: Modified `_core.py` (gl.call arg dispatch bypasses `to_tensor` for shared descriptors) and `_semantic.py` (relaxed isinstance, PaddedSharedLayout rejection guard, memory_space-keyed arg_params).
</objective>

<execution_context>
@/home/cicuvc/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/cicuvc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-CONTEXT.md
@.planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-RESEARCH.md
@.planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax gl.call() arg dispatching to bypass to_tensor for shared_memory_descriptor</name>
  <files>python/triton/experimental/gluon/language/_core.py</files>
  <read_first>
    - python/triton/experimental/gluon/language/_core.py (lines 775-811: gl.call function, line 803: to_tensor call)
    - python/triton/language/semantic.py (lines 118-127: to_tensor raises TypeError for non-tensor/scalar)
    - python/triton/experimental/gluon/language/__init__.py (line 32: shared_memory_descriptor export)
    - .planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-PATTERNS.md (Section 1: gl.call arg dispatching pattern)
  </read_first>
  <action>
    Per D-18: Replace the single list comprehension at `_core.py:803` that calls `_semantic.to_tensor(a)` on every arg — this raises `TypeError` for `shared_memory_descriptor` (not a tensor, not a scalar). Replace with a loop that passes through `ttgl.tensor` and `ttgl.shared_memory_descriptor` instances directly, only calling `_semantic.to_tensor()` on other arg types (scalar constants, etc.).

    The replacement preserves the existing behavior that the `tensors` list contains `.handle`-bearing objects (both `tensor` and `shared_memory_descriptor` have `.handle` attributes used downstream at `_semantic.py:313`). The `ttgl` module alias is already in scope via `import triton.experimental.gluon.language as ttgl`.

    Do NOT add new imports. Do NOT modify `call_extern()` at line 808 — just the arg preparation at line 803.
  </action>
  <verify>
    <automated>MISSING — verify manually: write a trivial Gluon kernel that calls `gl.call("tt_plugin.cu", "some_fn", shared_desc)` and confirm no `TypeError: cannot convert shared_memory_descriptor` is raised at `_core.py:803`. A unit test is not required — Phase 7 E2E tests exercise this path.</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '_semantic.to_tensor(a) for a in args' python/triton/experimental/gluon/language/_core.py` equals 0 (list comprehension replaced by loop)
    - Source assertion: `grep -c 'isinstance(a, (ttgl.tensor, ttgl.shared_memory_descriptor))' python/triton/experimental/gluon/language/_core.py` equals 1
    - Behavior assertion: passing a `shared_memory_descriptor` to `gl.call()` does NOT raise `TypeError` at `_core.py` — the descriptor reaches `call_extern()` with its `.handle` intact
  </acceptance_criteria>
  <done>gl.call() passes shared_memory_descriptor args through to call_extern without TypeError — the isinstance guard at _core.py:803 bypasses to_tensor for shared descriptors</done>
</task>

<task type="auto">
  <name>Task 2: Relax call_extern() isinstance guard and add PaddedSharedLayout rejection</name>
  <files>python/triton/experimental/gluon/language/_semantic.py</files>
  <read_first>
    - python/triton/experimental/gluon/language/_semantic.py (lines 250-319: call_extern method — isinstance check at 253-255, f64 guard at 259-263, arg_params at 270-276)
    - python/triton/experimental/gluon/language/_layouts.py (lines 174-213: PaddedSharedLayout class definition)
    - .planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-PATTERNS.md (Section 2: _semantic.py patterns A and B)
    - .planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-CONTEXT.md (D-18, D-19, D-20)
  </read_first>
  <action>
    Three modifications to `call_extern()` in `_semantic.py`, all within lines 250-319:

    **(a) Relax isinstance check (D-18):** Replace the isinstance call at lines 253-255 from `isinstance(a, ttgl.tensor)` to `isinstance(a, (ttgl.tensor, ttgl.shared_memory_descriptor))`. Update the error message from "all arguments must be tensors" to "all arguments must be tensors or shared_memory_descriptors".

    **(b) Add PaddedSharedLayout rejection guard (D-19/D-20):** After the f64 guard block (after line 263), insert a new guard that iterates over `args`, checks `isinstance(a, ttgl.shared_memory_descriptor)` and `isinstance(a.type.layout, _glayouts.PaddedSharedLayout)`, raising `NotImplementedError` with the message: "gl.call() does not support PaddedSharedLayout shared memory; use SharedLinearLayout/SwizzledSharedLayout/NVMMASharedLayout (see v1.1 out-of-scope)". Import `_layouts` as `_glayouts` — the `_layouts` module is already imported at the top of `_semantic.py` via `from triton.experimental.gluon.language import _layouts`.

    **(c) Add memory_space key to arg_params (D-14/D-21):** In the arg_params construction loop (lines 270-276), branch on `isinstance(a, ttgl.shared_memory_descriptor)`. For shared descriptors, append a dict with keys: `"dtype"` (str), `"shape"` (list), `"layout"` (the `a.type.layout` Python layout object per D-21), and `"memory_space": "shared"`. For distributed args, the existing dict (dtype, shape, layout) is unchanged — absence of `memory_space` signals register/distributed.
  </action>
  <verify>
    <automated>MISSING — verify manually: 
    (a) write a trivial Gluon kernel calling `gl.call()` with a `shared_memory_descriptor` arg — confirm no `_check` failure at `_semantic.py:253-255`
    (b) create a `shared_memory_descriptor` with `PaddedSharedLayout` and pass to `gl.call()` — confirm `NotImplementedError` is raised with the expected message
    (c) inspect `arg_params` content (e.g., via a temporary print) to confirm shared args have `memory_space: "shared"` and distributed args do not</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'isinstance(a, (ttgl.tensor, ttgl.shared_memory_descriptor))' python/triton/experimental/gluon/language/_semantic.py` equals 1
    - Source assertion: `grep -c 'PaddedSharedLayout' python/triton/experimental/gluon/language/_semantic.py` greater than or equal to 1 (PaddedSharedLayout guard exists)
    - Source assertion: `grep -c '"memory_space"' python/triton/experimental/gluon/language/_semantic.py` greater than or equal to 1 (memory_space key exists)
    - Source assertion: `grep -c '"shared"' python/triton/experimental/gluon/language/_semantic.py` greater than or equal to 1
    - Behavior assertion: `gl.call()` with a `shared_memory_descriptor` arg passes the isinstance check
    - Behavior assertion: `gl.call()` with a `PaddedSharedLayout` descriptor raises `NotImplementedError` matching D-20 message
  </acceptance_criteria>
  <done>call_extern() accepts shared_memory_descriptor args alongside tensors (D-18), rejects PaddedSharedLayout (D-19/D-20), and threads memory_space through arg_params (D-14/D-21)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gl.call() user API → _semantic.py | User-supplied args cross into IR builder; isinstance guards prevent type confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|----------------|
| T-06-01 | Spoofing | _semantic.py isinstance guard | medium | mitigate | isinstance check with explicit tuple `(ttgl.tensor, ttgl.shared_memory_descriptor)` rejects all other types before IR construction |
| T-06-02 | Tampering | _core.py arg dispatch | low | mitigate | `to_tensor()` is only called for non-tensor non-descriptor args; `shared_memory_descriptor` handles are never materialized through the tensor path |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed (Phase 6 modifies existing source files only) |
</threat_model>

<verification>
## Phase-Level Verification

- **Isinstance relaxation:** Confirm `gl.call()` no longer raises `TypeError` for `shared_memory_descriptor` args — exercised by Phase 7 E2E tests.
- **PaddedSharedLayout rejection:** Raise `NotImplementedError` with D-20 message — manually verified in this phase.
- **arg_params memory_space:** Shared args carry `"memory_space": "shared"`; downstream Plan 06-02 reads it.
</verification>

<success_criteria>
- `gl.call()` in `_semantic.py` accepts a `shared_memory_descriptor` alongside tensor arguments — no `TypeError` or validation rejection (ROADMAP success criterion 2)
- PaddedSharedLayout raises `NotImplementedError` before IR construction
</success_criteria>

<output>
Create `.planning/phases/06-cuda-wiring-llvm-lowering-frontend-api/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Modified files:
- `python/triton/experimental/gluon/language/_core.py` — `gl.call()` arg dispatching loop (isinstance bypass for `shared_memory_descriptor`)
- `python/triton/experimental/gluon/language/_semantic.py` — `call_extern()` isinstance relaxation, PaddedSharedLayout guard, arg_params memory_space key

No new files, classes, functions, or symbols.
