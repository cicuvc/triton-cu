---
phase: 03-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/test/gluon/tt_plugin.cu
  - python/test/gluon/test_extern_call.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "A CUDA device function `reduce_f16` exists in tt_plugin.cu returning Tensor<float, Shape<32>, TRes> from half input, accumulating in f32"
    - "A Gluon kernel `reduce_f16_kernel` calls gl.call(..., 'reduce_f16', ...) with ONLY result_layout=gl.SliceLayout(1, layout) — no hand-computed shape/dtype"
    - "test_reduce_f16_f32 passes — GPU output matches x.to(torch.float32).sum(1) within rtol=1e-2, atol=1e-2"
    - "test_reduce_f16_f32 asserts kernel.asm['ttgir'] contains both f32 element type and tensor<32xf32 result shape — proving inference produced the correct type before lowering"
    - "All 4 existing extern-call tests (test_elementwise_add, test_intra_warp_add_sibling, test_reduce_different_shape, test_split_add_tuple) pass unchanged"
    - "make test-lit exits 0 — lit suite is unaffected by this test-only phase"
  artifacts:
    - path: "python/test/gluon/tt_plugin.cu"
      provides: "reduce_f16 device function — f16→f32 reduction mirroring existing reduce"
      after_line: 151
    - path: "python/test/gluon/test_extern_call.py"
      provides: "reduce_f16_kernel + test_reduce_f16_f32 E2E test"
      after_line: 131
  key_links:
    - from: "python/test/gluon/test_extern_call.py (reduce_f16_kernel)"
      to: "python/test/gluon/tt_plugin.cu (reduce_f16)"
      via: "gl.call('python/test/gluon/tt_plugin.cu', 'reduce_f16', x_vals, result_layout=gl.SliceLayout(1, layout))"
    - from: "test_reduce_f16_f32 assertion"
      to: "Phase 2 inference pipeline (_semantic.py + clang_compiler.cc)"
      via: "kernel.asm['ttgir'] text-match proves CUDA-inferred dtype+shape reached the extern_call op result type"
---

<objective>
Add a new `f16 → f32` reduction device function to `tt_plugin.cu` and a matching E2E test in `test_extern_call.py` that proves the `gl.call()` return-type inference feature works for shape AND dtype transitions simultaneously. Run full regression gates (all 4 existing tests + lit suite) to confirm no breakage.

Purpose: Close the INFER-02 E2E coverage gap — all 4 existing tests are same-dtype (`T→T`); this is the first test exercising a dtype-changing extern call. One combined shape+dtype test satisfies TEST-01 (new test with only result_layout supplied), TEST-02 (existing tests pass), and TEST-03 (lit unaffected).
Output: One new device function in `tt_plugin.cu`, one new kernel + test function in `test_extern_call.py`, verified regression gates.
</objective>

<execution_context>
@/home/cicuvc/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/cicuvc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-verification/03-CONTEXT.md
@.planning/phases/03-verification/03-PATTERNS.md
@.planning/codebase/TESTING.md
@python/test/gluon/tt_plugin.cu
@python/test/gluon/test_extern_call.py
@AGENTS.md
</context>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `reduce_f16` | CUDA `__device__` function | `python/test/gluon/tt_plugin.cu` (after line 151) | f16→f32 reduction: `Tensor<half, Shape<32,32>, TArg>` → `Tensor<float, Shape<32>, TRes>`, f32 accumulator |
| `reduce_f16_kernel` | `@gluon.jit` kernel | `python/test/gluon/test_extern_call.py` (after `split_add_kernel`) | Gluon kernel calling `gl.call(..., "reduce_f16", ...)` with only `result_layout=gl.SliceLayout(1, layout)` |
| `test_reduce_f16_f32` | pytest test function | `python/test/gluon/test_extern_call.py` (after `test_split_add_tuple`) | E2E test: f16 input, f32 output, numeric check + inferred-type assertion on `kernel.asm['ttgir']` |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reduce_f16 device function to tt_plugin.cu</name>
  <files>python/test/gluon/tt_plugin.cu</files>

  <read_first>
  - `python/test/gluon/tt_plugin.cu` (entire file — 173 lines)
  - `.planning/phases/03-verification/03-PATTERNS.md` (analog: `reduce` at lines 142-151, layout aliases `TArg`/`TRes` at 138-139)
  - `.planning/phases/03-verification/03-CONTEXT.md` (D-02: exact signature, D-07: tolerance)
  </read_first>

  <action>
  Per D-02, append a new `__device__` function `reduce_f16` after the existing `reduce` (after line 151, before `split_add`). This function mirrors the existing `reduce` structurally — same layout aliases `TArg` (Shape<32,32>) and `TRes` (Shape<32>) from lines 138-139 — with three differences:

  1. Input param type: `const Tensor<half, Shape<32, 32>, TArg>& Vals` (replace templated `T` with `half`)
  2. Return type: `Tensor<float, Shape<32>, TRes>` (replace `Tensor<T, Shape<32>, TRes>` with concrete `float`)
  3. Accumulator init: `Result.data[0] = float{};` (replace `T{}` with `float{}`)

  Keep the `#pragma unroll` / `for` loop identical — `half` → `float` promotion is implicit in `Result.data[0] += Vals.data[i]`. Insert a one-line comment above the function: `// f16 -> f32 reduction: same layout structure as reduce, different element types`. Do NOT alter any existing code (the `TArg`/`TRes` aliases, the existing `reduce`, `split_add`, or anything else above/below).

  No `#include` changes required — `half` is a built-in CUDA type available in the device compilation environment.

  Exact function signature:
  ```
  __device__ Tensor<float, Shape<32>, TRes> reduce_f16(const Tensor<half, Shape<32, 32>, TArg>& Vals)
  ```

  The `PlaceholderLayout` probe at lines 81-99 reads the declared return type for inference — `Tensor<float, Shape<32>, TRes>` is what the CUDA-side inference will read as the return type, proving the f32 element type and [32] shape.
  </action>

  <verify>
    <automated>grep -n 'reduce_f16' python/test/gluon/tt_plugin.cu | grep -c '__device__' | grep -v '^0$'</automated>
  </verify>

  <acceptance_criteria>
  - `python/test/gluon/tt_plugin.cu` contains exactly one `__device__` function named `reduce_f16` returning `Tensor<float, Shape<32>, TRes>`
  - The function accepts `const Tensor<half, Shape<32, 32>, TArg>& Vals`
  - The function body uses `{}` zero-initialization and `#pragma unroll` / `for` loop identical to existing `reduce`
  - No existing functions, aliases, or definitions in `tt_plugin.cu` have been modified
  - `grep -c 'reduce_f16' python/test/gluon/tt_plugin.cu` reports the function name appears at least once in a declaration context (not just in comments)
  </acceptance_criteria>

  <done>Device function `reduce_f16` compiles as part of the CUDA device library, ready for JIT compilation when referenced by a test kernel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add reduce_f16_kernel and test_reduce_f16_f32 to test_extern_call.py</name>
  <files>python/test/gluon/test_extern_call.py</files>

  <read_first>
  - `python/test/gluon/test_extern_call.py` (entire file — 131 lines)
  - `.planning/phases/03-verification/03-PATTERNS.md` (analogs: `reduce_kernel` at lines 35-44, `test_reduce_different_shape` at 82-88, MLIR assertion pattern at 196-206, imports at 1-10)
  - `.planning/phases/03-verification/03-CONTEXT.md` (D-01: shape+dtype together, D-03: torch ref, D-04: only result_layout, D-05: inferred-type assertion, D-07: tolerance)
  - `.planning/codebase/TESTING.md` (pytest patterns, `torch.testing.assert_close`, `pytestmark` skipif)
  </read_first>

  <action>
  Append two new items to `test_extern_call.py` after the existing `split_add_kernel` (after line 58) and `test_split_add_tuple` (after line 101). Do NOT modify any existing code.

  **Step A — New kernel `reduce_f16_kernel` (append after `split_add_kernel`, before the test functions):**

  Copy the `reduce_kernel` structure (lines 36-44) exactly — same `BlockedLayout([1, 32], [32, 1], [1, 1], [1, 0])`, same 2D indexing + 1D slice pattern. Change the single `gl.call` string from `"reduce"` to `"reduce_f16"`. No other changes — the kernel itself is dtype-agnostic; the f16→f32 transition is entirely in the `.cu` device function.

  Kernel function name: `reduce_f16_kernel`
  Decorator: `@gluon.jit` (same as all existing kernels)

  **Step B — New test `test_reduce_f16_f32` (append after `test_split_add_tuple`, before `test_gl_call_no_inference_hook_raises`):**

  Per D-01 (shape+dtype together), D-03 (torch ref), D-04 (only result_layout), D-05 (inferred-type assertion), D-07 (tolerance):

  1. `torch.set_default_device('cuda')`
  2. Input: `x = torch.randn((32, 32), dtype=torch.float16)` — f16 input
  3. Output: `out = torch.empty((32,), dtype=torch.float32)` — f32 output
  4. Launch: `reduce_f16_kernel[(1,)](x, out, num_warps=1)` — only result_layout is supplied via kernel body; no shape/dtype passed from test
  5. Sync: `torch.cuda.synchronize()`
  6. Numeric check (D-03, D-07): `ref = x.to(torch.float32).sum(1)` then `torch.testing.assert_close(out, ref, rtol=1e-2, atol=1e-2)`
  7. Inferred-type assertion (D-05):
     - Read `ttgir = reduce_f16_kernel.asm["ttgir"]` — available after kernel invocation triggers compilation
     - Assert `"f32" in ttgir` with a message showing the failing IR
     - Assert `"tensor<32xf32" in ttgir` with a message showing the failing IR
     - These prove inference produced the correct element type and shape before lowering

  Add a docstring above the test function: `"""f16 -> f32 reduction: verifies shape AND dtype are inferred from CUDA side. Only result_layout is supplied; f32 element type + [32] shape come from inference."""`

  No import changes needed — `torch`, `pytest`, `gluon`, `gl`, and `is_cuda` are already imported. The `pytestmark` module-level skipif already gates the entire file.

  The existing `test_gl_call_no_inference_hook_raises` (line 104) must remain the last test in the file and must pass unchanged. Place the new test BEFORE it.
  </action>

  <verify>
    <automated>PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest python/test/gluon/test_extern_call.py::test_reduce_f16_f32 -xvs 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - `test_extern_call.py` contains `@gluon.jit` decorated `reduce_f16_kernel` with `gl.call(..., "reduce_f16", x_vals, result_layout=gl.SliceLayout(1, layout))`
  - `test_extern_call.py` contains `def test_reduce_f16_f32():` with f16 input, f32 output, `assert_close` with `rtol=1e-2, atol=1e-2`, and `kernel.asm["ttgir"]` assertions
  - No existing function modified; new kernel placed after `split_add_kernel`, new test placed after `test_split_add_tuple` and before `test_gl_call_no_inference_hook_raises`
  - `pytest python/test/gluon/test_extern_call.py::test_reduce_f16_f32` exits 0 with GPU present
  </acceptance_criteria>

  <done>New E2E test passes: f16→f32 reduction produces numerically correct results via inference, and the compiled kernel's ttgir confirms f32 element type + [32] shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Regression gates — existing tests + lit suite</name>
  <files>python/test/gluon/test_extern_call.py</files>

  <read_first>
  - `.planning/phases/03-verification/03-CONTEXT.md` (D-06: separate lit gate, D-02 constraint: new function must not alter existing `reduce` behavior)
  - `.planning/codebase/TESTING.md` (run commands: `make test-lit`, `pytest python/test/gluon/test_extern_call.py`)
  - `python/test/gluon/test_extern_call.py` (existing test names: `test_elementwise_add`, `test_intra_warp_add_sibling`, `test_reduce_different_shape`, `test_split_add_tuple`)
  - `AGENTS.md` (build note: `PYTHONPATH` required, CUDA GPU needed)
  </read_first>

  <action>
  **Gate A — All 5 extern-call tests (TEST-02):**

  Run the full test file to confirm the 4 existing tests + the new test all pass:
  ```
  PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest python/test/gluon/test_extern_call.py -xvs
  ```

  Expected: 5 tests collected, all pass. Exit code 0. If any existing test fails, stop and report the failure — the new device function/kernel must not have altered existing behavior (per D-02 constraint: new function is a separate symbol, existing `reduce` is untouched).

  **Gate B — lit suite (TEST-03, per D-06):**

  Run the MLIR lit test suite from the build directory:
  ```
  make test-lit
  ```
  (or, if build dir differs: `cd build && ninja triton-opt && lit -v test/`)

  Expected: all lit tests pass (exit code 0). No MLIR/dialect source changes in this phase, so no lit regressions are expected — this is a defensive confirmation per D-06. If any lit test fails, report the failure path and output; it may indicate a latent issue unrelated to this phase.

  **Gate C — Verify new test is collected:**

  Confirm the new test appears in pytest collection:
  ```
  PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest --co python/test/gluon/test_extern_call.py 2>&1 | grep -c 'test_reduce_f16_f32'
  ```
  Expected: reports 1 (exactly one test collected with this name).

  All three gates use the same `PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia"` environment prefix. Requires a CUDA GPU (tests are `skipif(not is_cuda())`).
  </action>

  <verify>
    <automated>PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest python/test/gluon/test_extern_call.py -x --tb=short 2>&1 | grep -E '(passed|failed|error)'</automated>
  </verify>

  <acceptance_criteria>
  - `pytest python/test/gluon/test_extern_call.py` exits 0 with 5 tests passed (test_elementwise_add, test_intra_warp_add_sibling, test_reduce_different_shape, test_split_add_tuple, test_reduce_f16_f32)
  - `make test-lit` exits 0 (all lit tests pass)
  - `pytest --co python/test/gluon/test_extern_call.py` output lists `test_reduce_f16_f32` in the collected tests
  </acceptance_criteria>

  <done>All regression gates pass: 4 existing tests + 1 new test all pass; lit suite unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a test-only + device-library-addition phase. The device function (`reduce_f16`) runs in the same CUDA kernel context as existing device functions; the test code runs in the same pytest process as existing tests. No external input, no network surface, no user data, no secrets.

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundaries in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `test_extern_call.py` assertion messages | low | accept | Assertion failure messages include the full `ttgir` IR dump for debugging — this is test-only output, no production data exposure. Accept: test diagnostic information is intentionally verbose for developer debugging. |
| T-03-02 | Denial of Service | `tt_plugin.cu` `reduce_f16` accumulator | low | accept | Device function performs 32 iterations of `+=` — same computational cost as existing `reduce`. No unbounded loops, no heap allocation. Accept: trusted input within the CUDA kernel; cost equal to approved existing functions. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed in this phase — zero `pip install`, `npm install`, or `cargo install` calls. Device function and test are pure source additions to existing files. |
</threat_model>

<verification>
### Phase-level verification

1. **Full extern-call test suite:**
   ```bash
   PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest python/test/gluon/test_extern_call.py -xvs
   ```
   → 5 tests pass, exit 0 (TEST-01 + TEST-02)

2. **New test isolation:**
   ```bash
   PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python3 -m pytest python/test/gluon/test_extern_call.py::test_reduce_f16_f32 -xvs
   ```
   → single test passes with both numeric and inferred-type assertions satisfied (TEST-01)

3. **Lit regression guard:**
   ```bash
   make test-lit
   ```
   → all lit tests pass, exit 0 (TEST-03)

4. **Device function integrity:**
   ```bash
   grep -A 12 'reduce_f16' python/test/gluon/tt_plugin.cu
   ```
   → confirms signature `Tensor<float, Shape<32>, TRes>`, param `Tensor<half, Shape<32, 32>, TArg>`, and `{}` accumulator init
</verification>

<success_criteria>
- [ ] `reduce_f16` device function exists in `tt_plugin.cu` (after line 151, before `split_add`)
- [ ] `reduce_f16_kernel` + `test_reduce_f16_f32` exist in `test_extern_call.py` (before `test_gl_call_no_inference_hook_raises`)
- [ ] `test_reduce_f16_f32` passes with f16 input, f32 output, `rtol=1e-2, atol=1e-2`
- [ ] `kernel.asm['ttgir']` assertions confirm `f32` element type and `tensor<32xf32` result shape
- [ ] All 4 existing extern-call tests pass unchanged (5 total tests passing)
- [ ] `make test-lit` exits 0
- [ ] No changes to `_semantic.py`, `clang_compiler.cc`, `compiler.py`, or any production source — test-only + device-library
</success_criteria>

<output>
Create `.planning/phases/03-verification/03-01-SUMMARY.md` when done
</output>
