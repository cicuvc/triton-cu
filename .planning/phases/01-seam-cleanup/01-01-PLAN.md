---
phase: 01-seam-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - third_party/nvidia/backend/compiler.py
  - python/triton/experimental/gluon/language/_semantic.py
autonomous: true
requirements:
  - BUG-01
  - BUG-02
must_haves:
  truths:
    - "f64/fp64/float64 args to gl.call() raise NotImplementedError before any IR is built (backend-agnostic dtype-string check, no CUDA import)"
    - "dtype_to_scalar at compiler.py:538-545 no longer maps 'f64'/'fp64' to llvm.ScalarType.Fp32 — they raise NotImplementedError via _scalar_type_for helper"
    - "The unreachable duplicate return block at compiler.py:510-513 does not exist"
    - "All 4 existing extern-call tests pass unchanged: pytest python/test/gluon/test_extern_call.py exits 0"
  artifacts:
    - "third_party/nvidia/backend/compiler.py — dead code removed (lines 510-513), new _scalar_type_for helper replacing silent f64→Fp32 coercion"
    - "python/triton/experimental/gluon/language/_semantic.py — f64 early guard inserted before first_input assignment"
  key_links:
    - "call_extern f64 guard → raises before result_types IR build (prevents invalid IR)"
    - "dtype_to_scalar → _scalar_type_for → error for f64/fp64 (backend backstop)"
  prohibitions:
    - "call_extern must NOT consume the inference hook to build result types in Phase 1 (still infers from first_input)"
    - "No silent f64/fp64 → Fp32 coercion remains anywhere in the pipeline"
    - "No new tests added in Phase 1 (tests are Phase 3)"
---

<objective>
Remove dead unreachable code and add defense-in-depth f64/fp64 guards at both the backend-agnostic Gluon semantic layer and the CUDA backend — clearing BUG-01 and BUG-02 before the inference seam work begins.

Purpose: Eliminate silent Fp32 truncation of f64 inputs and clean up dead code that confuses the compiler pipeline. The f64 guard is a backend-agnostic dtype-string check preserving frontend/backend layering.

Output: Cleaned compiler.py with no dead code and no silent f64→Fp32 coercion; _semantic.py with an early f64 guard that raises before IR build.
</objective>

<execution_context>
@/home/cicuvc/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/cicuvc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-seam-cleanup/01-CONTEXT.md
@.planning/phases/01-seam-cleanup/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead unreachable code (BUG-01, D-10)</name>
  <files>third_party/nvidia/backend/compiler.py</files>
  <read_first>
- third_party/nvidia/backend/compiler.py:500-513 (the dead code block)
- .planning/phases/01-seam-cleanup/01-PATTERNS.md section 5 (lines 234-252) for the exact before/after
- .planning/phases/01-seam-cleanup/01-CONTEXT.md D-10 (mechanical delete)
  </read_first>
  <action>
Delete lines 510-513 in third_party/nvidia/backend/compiler.py — the four-line unreachable duplicate block after the unconditional `return ret` at line 508:

```
        ret = str(llvm_mod)          # UNREACHABLE — line 510
        del llvm_mod                 # UNREACHABLE — line 511
        del context                  # UNREACHABLE — line 512
        return ret                   # UNREACHABLE — line 513
```

The `return ret` at line 508 already returns unconditionally from `make_llir`. These four lines are dead code that will never execute. Per D-10, this is a mechanical removal — no logic change, no verification beyond the file diff. Ensure the file ends cleanly after line 508 (the blank line before `def _pre_compile_extern_calls` at line 515 must remain).
  </action>
  <verify>
    <automated>grep -n 'del llvm_mod' third_party/nvidia/backend/compiler.py</automated>
  </verify>
  <acceptance_criteria>
- Source: `grep -c 'del llvm_mod' third_party/nvidia/backend/compiler.py` equals 1 (the first, legitimate `del llvm_mod` at line 506 — NOT 2 anymore)
- Source: Lines 510-513 of the original file no longer contain any code (verified by diff)
- Source: `def _pre_compile_extern_calls` at line ~511 (shifted up by 4 lines from original line 515)
  </acceptance_criteria>
  <done>Dead code block removed; the single `return ret` at the end of make_llir is the only exit path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add f64/fp64 guard — both layers (BUG-02, D-08, D-09)</name>
  <files>
    third_party/nvidia/backend/compiler.py
    python/triton/experimental/gluon/language/_semantic.py
  </files>
  <read_first>
- python/triton/experimental/gluon/language/_semantic.py:250-273 (current call_extern body)
- third_party/nvidia/backend/compiler.py:538-545 (current dtype_to_scalar with silent coercion)
- .planning/phases/01-seam-cleanup/01-PATTERNS.md section 3 (lines 93-137) for f64 guard pattern in _semantic.py
- .planning/phases/01-seam-cleanup/01-PATTERNS.md section 4 (lines 153-186) for dtype_to_scalar replacement pattern
- .planning/phases/01-seam-cleanup/01-CONTEXT.md D-08 (early dtype-string check, backend-agnostic, NO CUDA import)
- .planning/phases/01-seam-cleanup/01-CONTEXT.md D-09 (both layers: _semantic.py + compiler.py)
- .planning/phases/01-seam-cleanup/01-CONTEXT.md layering_constraint (Gluon semantic layer must NOT import NVIDIA backend code)
  </read_first>
  <action>
**Layer 1 (D-08): Early f64 guard in call_extern (_semantic.py)**

Insert an f64/fp64/float64 guard in `call_extern` (`_semantic.py`) AFTER the existing argument validation block (lines 253-257) and BEFORE the `first_input = args[0]` line (currently line 259). The guard must:
- Be a pure dtype-string check: iterate `args`, check `str(a.dtype) in ("fp64", "f64", "float64")`
- Use `hasattr(a, 'dtype')` for safety
- Raise `NotImplementedError("gl.call() does not support float64; full Fp64 support is out of scope (see FP64-01)")`
- NOT import any CUDA/NVIDIA module — this is a backend-agnostic check
- NOT use any Triton dtype objects — strings only

**Layer 2 (D-09): Backend backstop in compiler.py**

Replace the f64/fp64 silent-coercion rows in `dtype_to_scalar` (currently lines 542-543: `"f64": llvm.ScalarType.Fp32, "fp64": llvm.ScalarType.Fp32`) with a new helper function `_scalar_type_for(dtype_str)`.

1. Remove the f64/fp64 entries from `dtype_to_scalar` dict entirely (delete lines 542-543).
2. Add a new function `_scalar_type_for` immediately after the `dtype_to_scalar` dict definition. Pattern from 01-PATTERNS.md section 4 lines 176-183:
   - Look up `dtype_str` in `dtype_to_scalar`
   - If found, return the scalar type
   - If not found AND `dtype_str in ("f64", "fp64", "float64")`: raise `NotImplementedError("gl.call() does not support float64; full Fp64 support is out of scope (see FP64-01)")`
   - Otherwise: raise `ValueError(f"Unsupported dtype: {dtype_str}")`
3. Replace the call site `dtype_to_scalar.get(inp["dtype"], llvm.ScalarType.Fp32)` at line 572 with `_scalar_type_for(inp["dtype"])`.

The f64 error message must be identical across both layers so grep-able (`"float64"` substring).
  </action>
  <verify>
    <automated>grep -c 'f64.*Fp32\|fp64.*Fp32' third_party/nvidia/backend/compiler.py</automated>
  </verify>
  <acceptance_criteria>
- Source: In compiler.py, `grep -c 'f64.*Fp32\|fp64.*Fp32'` returns 0 (no silent coercion remains)
- Source: In compiler.py, `_scalar_type_for` function exists and raises NotImplementedError for "f64", "fp64", "float64"
- Source: In _semantic.py, `call_extern` contains a dtype-string check for f64/fp64/float64 BEFORE the `first_input = args[0]` line
- Source: In _semantic.py, the guard DOES NOT import triton._C.libtriton or any nvidia module (grep for `import.*nvidia\|import.*libtriton` in the guard lines)
- Behavior: `pytest python/test/gluon/test_extern_call.py` exits 0 (all 4 existing tests pass — they use f32/f16 only)
- Behavior: A synthetic gl.call with an f64 arg raises NotImplementedError containing "float64" before the old dtype_to_scalar path would have silently coerced
  </acceptance_criteria>
  <done>f64/fp64 args to gl.call raise NotImplementedError at the Gluon semantic layer before IR build; the CUDA backend dtype_to_scalar also raises (defense in depth). No silent Fp32 truncation anywhere. Existing f32/f16 tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user Python call → call_extern | f64 dtype args rejected before IR build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | dtype_to_scalar dict | low | mitigate | Replace silent-fallback with explicit error in \_scalar_type_for; no silent truncation |
| T-01-02 | Information Disclosure | call_extern error message | low | accept | Error message is user-facing; contains no secrets; acceptable for debugging |
</threat_model>

<verification>
## Regression Gate

```bash
PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" pytest python/test/gluon/test_extern_call.py -x --tb=short
```

Must exit 0. All 4 existing tests (elementwise_add, intra_warp_add_sibling, reduce, split_add) pass unchanged.

## Source Assertions

```bash
# No f64→Fp32 silent coercion
grep -c 'f64.*Fp32\|fp64.*Fp32' third_party/nvidia/backend/compiler.py  # expect 0

# Dead code removed — only 1 del llvm_mod (the legitimate one)
grep -c 'del llvm_mod' third_party/nvidia/backend/compiler.py  # expect 1

# f64 guard present in _semantic.py
grep -c 'float64' python/triton/experimental/gluon/language/_semantic.py  # expect >= 1
```
</verification>

<success_criteria>
1. Dead code at compiler.py:510-513 is removed.
2. f64/fp64/float64 raises NotImplementedError at both layers (early guard + backend backstop).
3. No silent f64→Fp32 coercion anywhere.
4. All 4 existing extern-call tests pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-seam-cleanup/01-01-SUMMARY.md` when done
</output>
