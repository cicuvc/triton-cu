---
phase: 05-mlir-op-relaxation-spec-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td
  - test/TritonGPU/extern-call-mixed-inputs.mlir
  - test/TritonGPU/extern-call-tensor-only.mlir
autonomous: true
requirements:
  - SHMLIR-01

must_haves:
  truths:
    - A mixed tensor+memdesc ttg.extern_call parses through triton-opt --verify-diagnostics without type constraint error
    - A tensor-only ttg.extern_call continues to parse through triton-opt --verify-diagnostics without error (regression)
    - The ODS operand constraint on line 803 reads exactly Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs
    - Existing Gluon lit suite (test/TritonGPU/) passes unchanged — no regression in any unrelated lit test
  artifacts:
    - include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td (modified line 803)
    - test/TritonGPU/extern-call-mixed-inputs.mlir (new file)
    - test/TritonGPU/extern-call-tensor-only.mlir (new file)
  key_links:
    - TritonGPUOps.td:803 → grep confirms AnyTypeOf<[TT_Tensor, TTG_MemDescType]>
    - Mixed lit test RUN line → triton-opt --verify-diagnostics exits 0
    - Tensor-only lit test RUN line → triton-opt --verify-diagnostics exits 0
    - Full TritonGPU lit suite → no new failures introduced
---

<objective>
Relax the `ttg.extern_call` ODS operand constraint from `Variadic<TT_Tensor>` to `Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>`, then create two lit tests proving (a) mixed tensor+memdesc inputs parse successfully, and (b) tensor-only inputs still parse without regression.

Purpose: Enable MLIR parsers to accept `MemDescType` operands on `extern_call` ops — the precondition for Phase 6's shared-memory lowering, per D-09.
Output: Modified ODS at `TritonGPUOps.td:803`; lit tests at `test/TritonGPU/extern-call-mixed-inputs.mlir` and `test/TritonGPU/extern-call-tensor-only.mlir`.
</objective>

<execution_context>
@/home/cicuvc/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/cicuvc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-CONTEXT.md
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-RESEARCH.md
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-PATTERNS.md
@include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td
@include/triton/Dialect/Triton/IR/TritonOps.td
@test/TritonGPU/invalid-attributes.mlir
@test/TritonGPU/ops.mlir
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax ODS — AnyTypeOf at TritonGPUOps.td:803 (D-09)</name>
  <files>include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td</files>
  <read_first>
    - include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td (lines 786–814, the target op definition)
    - include/triton/Dialect/Triton/IR/TritonOps.td (lines 870–880, the existing AnyTypeOf analogue at line 877)
  </read_first>
  <behavior>
    - Test 1: the replacement string `Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>` occurs exactly once in the file after the edit — verified by grep
    - Test 2: the old string `Variadic<TT_Tensor>:$inputs` no longer appears in the file
    - Test 3: grep confirms the line reads `    Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs,`
  </behavior>
  <action>
    At `TritonGPUOps.td` line 803, replace:
    `    Variadic<TT_Tensor>:$inputs,`
    with:
    `    Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs,`

    Per D-09: single mixed input list with per-operand type branching — MLIR-idiomatic, preserves natural arg ordering. The `functional-type($inputs, $results)` assembly format on line 812 already handles mixed types; no format change needed. `SameVariadicOperandSize` trait on line 787 validates operand count consistency without change. The `TTG_Op` base class auto-appends `VerifyTensorLayoutsTrait` and `VerifyMemDescLayoutsTrait` — no verifier changes needed.

    All required includes already present: `TritonGPUTypes.td` (line 6, provides `TTG_MemDescType`), `TritonTypes.td` (line 11, provides `TT_Tensor`), `mlir/IR/OpBase.td` (line 14, provides `AnyTypeOf`). Do NOT add, remove, or modify any include or other ODS line.
  </action>
  <verify>
    <automated>grep -n 'Variadic<AnyTypeOf<\[TT_Tensor, TTG_MemDescType\]>>' include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Variadic<TT_Tensor>:$inputs' include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` returns 0
    - `grep -c 'AnyTypeOf<\[TT_Tensor, TTG_MemDescType\]>' include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` returns 1
    - The replacement line is at (or near) line 803
  </acceptance_criteria>
  <done>The ODS operand line reads `Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs,` — only that token changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create lit tests — mixed-inputs + tensor-only regression (D-11, SHMLIR-01)</name>
  <files>test/TritonGPU/extern-call-mixed-inputs.mlir, test/TritonGPU/extern-call-tensor-only.mlir</files>
  <read_first>
    - test/TritonGPU/invalid-attributes.mlir (line 1: RUN line convention with -verify-diagnostics)
    - test/TritonGPU/ops.mlir (lines 89–97: memdesc syntax pattern using #shared and #smem attributes, module wrapping convention)
    - include/triton/Dialect/TritonGPU/IR/TritonGPUTypes.td (line 82: MemDescType hasCustomAssemblyFormat = 1)
  </read_first>
  <action>
    Create two new lit test files under `test/TritonGPU/`.

    **File 1: `test/TritonGPU/extern-call-tensor-only.mlir`**

    Tensor-only regression — proves AnyTypeOf does not reject pure tensor operands. Use the `-verify-diagnostics` RUN line (no expected-error markers = parse must succeed). Include exactly one `tt.func` inside a `module` with target attributes.

    MLIR content: a `ttg.extern_call` with two tensor operands (both `tensor<32x64xf32, #blocked>`), one result (same type), wrapped in a module with `"ttg.target" = "cuda:0"`, `"ttg.num-ctas" = 1 : i32`, `"ttg.num-warps" = 1 : i32`, `"ttg.threads-per-warp" = 32 : i32`. Use `#blocked` encoding: `#ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [4, 8], warpsPerCTA = [2, 2], order = [1, 0]}>`. Symbol: `"elementwise_add"`, libpath: `"tt_plugin.cu"`. Include a `// CHECK: ttg.extern_call` line before the op.

    **File 2: `test/TritonGPU/extern-call-mixed-inputs.mlir`**

    Mixed tensor+memdesc parse — proves ODS relaxation works for both operand types simultaneously. Same RUN line and module-wrapping convention as tensor-only test. First operand: `tensor<32x64xf32, #blocked>` (same encoding). Second operand: `!ttg.memdesc<32x64xf32, #shared, #smem>` using `#ttg.shared_linear` encoding and `#ttg.shared_memory` memory space.

    Use shared linear encoding: `#shared = #ttg.shared_linear<{offset = [[0, 1], [0, 2], [1, 0], [2, 0]], block = [[2, 0]]}, alignment = 16>`. Use `#smem = #ttg.shared_memory`. Symbol: `"test_fn"`, libpath: `"test.cu"`. One result: `tensor<32x64xf32, #blocked>`. Include a `// CHECK: ttg.extern_call` line.

    Per D-11: parse-only tests using `-verify-diagnostics` — no FileCheck field verification or extraction smoke testing. Both tests verify the ODS type constraint passes without error during parse verification.

    Write comments at the top of each file explaining what the test validates and that it's for SHMLIR-01 regression.
  </action>
  <verify>
    <automated>ls -la test/TritonGPU/extern-call-mixed-inputs.mlir test/TritonGPU/extern-call-tensor-only.mlir</automated>
  </verify>
  <acceptance_criteria>
    - Both `test/TritonGPU/extern-call-mixed-inputs.mlir` and `test/TritonGPU/extern-call-tensor-only.mlir` exist
    - Each file starts with `// RUN: triton-opt %s -split-input-file -verify-diagnostics`
    - Each file contains exactly one `tt.func` inside a `module` block with target attributes
    - Mixed test references `!ttg.memdesc` with `#ttg.shared_linear` encoding
    - Tensor-only test has no `!ttg.memdesc` references (pure tensor)
    - Both files have a `// CHECK: ttg.extern_call` line before the op
  </acceptance_criteria>
  <done>Two lit test files exist with correct RUN lines, module wrapping, and operand types — ready for verification after triton-opt rebuild.</done>
</task>

<task type="auto">
  <name>Task 3: Build triton-opt + run lit test verification</name>
  <files>test/TritonGPU/extern-call-mixed-inputs.mlir, test/TritonGPU/extern-call-tensor-only.mlir</files>
  <read_first>
    - AGENTS.md (build instructions: bash build.sh, PYTHONPATH-based dev, self-compiled LLVM path)
    - test/lit.cfg.py (lit configuration)
  </read_first>
  <action>
    Rebuild `triton-opt` after the ODS tablegen change: `cd $BUILD_DIR && ninja triton-opt`. This triggers tablegen re-generation from the modified `.td` file and recompiles the dialect. **Expect 2-5 minutes** for the incremental build (tablegen + affected TU recompilation).

    Run the two new lit tests individually:
    - `cd $BUILD_DIR && lit -v test/TritonGPU/extern-call-mixed-inputs.mlir`
    - `cd $BUILD_DIR && lit -v test/TritonGPU/extern-call-tensor-only.mlir`

    Both must exit 0 and print "PASS". 

    Run the full TritonGPU lit suite to confirm no regression:
    - `cd $BUILD_DIR && lit -v test/TritonGPU/`

    All existing tests must pass. If any unrelated test fails, investigate whether the ODS `AnyTypeOf` change caused a type constraint issue in a downstream verifier — the `Variadic` blast radius concern from STATE.md.
  </action>
  <verify>
    <automated>cd $BUILD_DIR && ninja triton-opt && lit -v test/TritonGPU/extern-call-mixed-inputs.mlir && lit -v test/TritonGPU/extern-call-tensor-only.mlir</automated>
  </verify>
  <acceptance_criteria>
    - `ninja triton-opt` exits 0 (no compile errors from regenerated tablegen output)
    - `lit -v test/TritonGPU/extern-call-mixed-inputs.mlir` exits 0 and prints PASS
    - `lit -v test/TritonGPU/extern-call-tensor-only.mlir` exits 0 and prints PASS
    - Full `lit -v test/TritonGPU/` shows zero new failures vs. pre-change baseline
  </acceptance_criteria>
  <done>Both new lit tests pass; full TritonGPU lit suite has zero new failures (ODS relaxation verified, SHMLIR-01 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLIR parser → generated verifier | Untrusted MLIR text crosses into parser; ODS type constraint enforces valid operand types |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | ODS type constraint | low | mitigate | AnyTypeOf rejects invalid operand types at parse time; SameVariadicOperandSize checks operand count — both auto-generated by tablegen. No custom verifier that could crash on unexpected types. |
| T-05-02 | Information Disclosure | Assembly format printer | low | accept | functional-type printer emits operand types in MLIR text — this is standard MLIR behavior and intentional for debuggability. No sensitive data in compiler IR. |
| T-05-SC | Tampering | build/cache | low | accept | No external packages installed. Internal tablegen + C++ change only. |
</threat_model>

<verification>
  - `ninja triton-opt` succeeds (tablegen + C++ rebuild)
  - `lit -v test/TritonGPU/extern-call-mixed-inputs.mlir` passes
  - `lit -v test/TritonGPU/extern-call-tensor-only.mlir` passes
  - Full `lit -v test/TritonGPU/` shows zero new failures
</verification>

<success_criteria>
  - `TritonGPUOps.td:803` reads `Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs`
  - Mixed tensor+memdesc extern_call parses without verifier error
  - Tensor-only extern_call parses unchanged (regression-free)
  - All existing TritonGPU lit tests pass
</success_criteria>

<output>
Create `.planning/phases/05-mlir-op-relaxation-spec-extraction/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | Location | Purpose |
|--------|------|----------|---------|
| `Variadic<AnyTypeOf<[TT_Tensor, TTG_MemDescType]>>:$inputs` | ODS constraint | `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td:803` | Relaxes `ttg.extern_call` to accept both tensor and memdesc operands (D-09) |
| `test/TritonGPU/extern-call-mixed-inputs.mlir` | Lit test file | `test/TritonGPU/` | Verifies mixed tensor+memdesc parse (SHMLIR-01, D-11) |
| `test/TritonGPU/extern-call-tensor-only.mlir` | Lit test file | `test/TritonGPU/` | Verifies tensor-only regression parse (SHMLIR-01, D-11) |
