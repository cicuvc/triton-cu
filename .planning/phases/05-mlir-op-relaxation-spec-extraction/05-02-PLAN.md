---
phase: 05-mlir-op-relaxation-spec-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/clang_compiler.cc
autonomous: true
requirements:
  - SHMLIR-02

must_haves:
  truths:
    - extractExternCallSpecs() no longer calls cast<RankedTensorType> on operand types — uses dyn_cast with a MemDescType branch
    - The shared-layout branch extracts offset_bases and block_bases from LinearLayout dims named "offset" and "block"
    - The shared-layout branch extracts alignment via SharedEncodingTrait::getAlignment()
    - JSON output for tensor inputs is bitwise identical to pre-change output (distributed-layout fields unchanged)
    - JSON output for shared inputs contains "memory_space": "shared", "offset_bases", "block_bases", "alignment" keys
    - The build completes without compile errors (bash build.sh exits 0)
  artifacts:
    - python/src/clang_compiler.cc (modified: structs at 1419–1435, extraction at 1438–1507, JSON at 1514–1567)
  key_links:
    - grep 'cast<RankedTensorType>' python/src/clang_compiler.cc in extractExternCallSpecs range → returns 0 (all casts replaced)
    - grep 'dyn_cast<MemDescType>' python/src/clang_compiler.cc → returns 1 (new shared-memory branch)
    - grep '"memory_space"' python/src/clang_compiler.cc → returns 1 (new JSON key in shared branch)
    - grep 'SharedEncodingTrait' python/src/clang_compiler.cc → returns 1 (alignment extraction)
---

<objective>
Replace the single `SpecInput` struct with `std::variant<TensorSpecInput, SharedSpecInput>`, add a `dyn_cast<MemDescType>` branch in `extractExternCallSpecs()` that extracts shared-layout data via `toLinearLayout(MemDescType)`, and update `tritonExtractExternCallSpecs()` to serialize variants with `std::visit`.

Purpose: Eliminate the `cast<RankedTensorType>` crash on `MemDescType` operands and emit shared-layout JSON keys (`memory_space`, `offset_bases`, `block_bases`, `alignment`) for downstream Phase 6 consumption, per D-10.
Output: Modified `clang_compiler.cc` with variant-based data model, branched extraction, and type-safe JSON serialization.
</objective>

<execution_context>
@/home/cicuvc/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/cicuvc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-CONTEXT.md
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-RESEARCH.md
@.planning/phases/05-mlir-op-relaxation-spec-extraction/05-PATTERNS.md
@python/src/clang_compiler.cc
@python/src/clang_compiler.h
@lib/Dialect/TritonGPU/IR/LinearLayoutConversions.cpp
@python/src/gluon_ir.cc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace SpecInput structs with variant model (D-10)</name>
  <files>python/src/clang_compiler.cc</files>
  <read_first>
    - python/src/clang_compiler.cc (lines 1415–1435, the anonymous namespace with SpecInput and ExternCallSpec)
    - python/src/clang_compiler.h (line 36, confirms #include <variant> is already present)
  </read_first>
  <behavior>
    - Test 1: `grep -c 'struct TensorSpecInput' python/src/clang_compiler.cc` returns 1
    - Test 2: `grep -c 'struct SharedSpecInput' python/src/clang_compiler.cc` returns 1
    - Test 3: `grep -c 'struct SpecInput' python/src/clang_compiler.cc` returns 0 (old struct removed)
    - Test 4: `grep -c 'std::variant<TensorSpecInput, SharedSpecInput>' python/src/clang_compiler.cc` returns 1 (in ExternCallSpec::inputs)
    - Test 5: `grep -c 'TensorSpecInput' python/src/clang_compiler.cc` returns >= 4 (struct definition + any references)
  </behavior>
  <action>
    In the anonymous namespace at lines 1419–1435 of `clang_compiler.cc`, replace the existing `SpecInput` struct (line 1421) and `ExternCallSpec` struct (line 1430) with the variant-based model.

    Delete `struct SpecInput` entirely (lines 1421–1428). Create two replacement structs and update ExternCallSpec:

    **Struct 1 — `TensorSpecInput`:** Same fields as old `SpecInput` but renamed for clarity. Fields: `std::string dtype`, `llvm::SmallVector<int64_t, 4> shape`, `int64_t numWarps`, `llvm::SmallVector<int32_t, 16> regBases`, `llvm::SmallVector<int32_t, 16> laneBases`, `llvm::SmallVector<int32_t, 16> warpBases`. These match the old struct's fields exactly.

    **Struct 2 — `SharedSpecInput`:** New struct carrying shared-memory layout data. Fields: `std::string dtype`, `llvm::SmallVector<int64_t, 4> shape`, `std::string memorySpace`, `llvm::SmallVector<int32_t, 16> offsetBases`, `llvm::SmallVector<int32_t, 16> blockBases`, `int32_t alignment`.

    **Updated `ExternCallSpec`:** Change `inputs` field from `llvm::SmallVector<SpecInput, 4> inputs` to `llvm::SmallVector<std::variant<TensorSpecInput, SharedSpecInput>, 4> inputs`. Keep `symbol`, `libpath`, and `useFastMath` fields unchanged.

    Per D-10: clean type-level separation between distributed and shared layout data. Do NOT add optional fields to a single struct — use the variant approach exclusively. All structs remain in the anonymous namespace.

    Do NOT add `#include <variant>` — already present in `clang_compiler.h:36`. No new includes needed.
  </action>
  <verify>
    <automated>grep -n 'struct TensorSpecInput\|struct SharedSpecInput\|std::variant<TensorSpecInput' python/src/clang_compiler.cc</automated>
  </verify>
  <acceptance_criteria>
    - `struct TensorSpecInput` exists with fields: `dtype`, `shape`, `numWarps`, `regBases`, `laneBases`, `warpBases` — source assertion via grep
    - `struct SharedSpecInput` exists with fields: `dtype`, `shape`, `memorySpace`, `offsetBases`, `blockBases`, `alignment` — source assertion via grep
    - `struct SpecInput` no longer exists in the file — `grep -c 'struct SpecInput' python/src/clang_compiler.cc` returns 0
    - `ExternCallSpec::inputs` type is `SmallVector<std::variant<TensorSpecInput, SharedSpecInput>, 4>` — grep confirms
    - Both structs are inside `namespace { }` (anonymous namespace, same as old structs)
  </acceptance_criteria>
  <done>Variant-based data model replaces the monolithic SpecInput — `TensorSpecInput` for distributed, `SharedSpecInput` for shared, `std::variant` in ExternCallSpec::inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MemDescType branch in extractExternCallSpecs()</name>
  <files>python/src/clang_compiler.cc</files>
  <read_first>
    - python/src/clang_compiler.cc (lines 1437–1507, the full extractExternCallSpecs function)
    - python/src/clang_compiler.cc (lines 1419–1435, the new variant structs from Task 1)
    - lib/Dialect/TritonGPU/IR/LinearLayoutConversions.cpp (lines 1323–1401, toLinearLayout overload signatures)
    - python/src/gluon_ir.cc (lines 238–242, shared dim names: "offset", "block")
  </read_first>
  <behavior>
    - Test 1: `grep -c 'cast<RankedTensorType>' python/src/clang_compiler.cc` returns 0 (no blind cast left in extraction)
    - Test 2: `grep -c 'dyn_cast<MemDescType>' python/src/clang_compiler.cc` returns 1 (new shared branch)
    - Test 3: `grep -c 'kOffset' python/src/clang_compiler.cc` returns ≥ 2 (declaration + usage)
    - Test 4: `grep -c 'kBlock' python/src/clang_compiler.cc` returns ≥ 2 (declaration + usage)
    - Test 5: `grep -c 'SharedEncodingTrait' python/src/clang_compiler.cc` returns 1 (alignment extraction)
    - Test 6: `grep -c 'SharedSpecInput' python/src/clang_compiler.cc` returns ≥ 3 (struct definition + new code + existing references)
  </behavior>
  <action>
    In `extractExternCallSpecs()` (starts at line 1438), make three coordinated changes:

    **1. Add shared dim-name constants** (adjacent to existing `kRegister`/`kLane`/`kWarp` at lines 1442–1447):
    ```
    StringAttr kOffset = StringAttr::get(module.getContext(), "offset");
    StringAttr kBlock  = StringAttr::get(module.getContext(), "block");
    ```
    Source of truth: `gluon_ir.cc:238–242` uses `"offset"` and `"block"` as MLIR dim names for shared layouts.

    **2. Extract `mapDtype` lambda** (before the per-operand loop, after dim-name constants):
    ```
    auto mapDtype = [](mlir::Type elemTy) -> std::string {
      if (isa<Float32Type>(elemTy))  return "f32";
      if (isa<Float64Type>(elemTy))  return "f64";
      if (isa<Float16Type>(elemTy))  return "f16";
      if (isa<BFloat16Type>(elemTy)) return "bf16";
      if (elemTy.isInteger(32))      return "i32";
      if (elemTy.isInteger(64))      return "i64";
      if (elemTy.isInteger(8))       return "i8";
      return "f32";
    };
    ```
    This replaces the duplicated dtype-mapping chain currently at lines 1481–1497, shared by both branches.

    **3. Replace the per-operand loop body** (lines 1455–1500) with a dyn_cast branch:

    Change `auto tensorTy = cast<RankedTensorType>(operand.getType());` on line 1456 to:
    ```
    auto type = operand.getType();
    ```

    Then split into two branches:

    **Branch A — `if (auto tensorTy = dyn_cast<RankedTensorType>(type))`:** Keep the existing distributed-layout path nearly verbatim. Shape from `tensorTy.getShape()`, encoding from `tensorTy.getEncoding()`, `toLinearLayout(shape, encoding)`. Push `TensorSpecInput` (not old `SpecInput`) with fields: `shape` assigned, `numWarps = ll.getInDimSize(kWarp)`, `regBases = flattenBases(ll.getBases().lookup(kRegister))`, `laneBases` and `warpBases` similarly, `dtype = mapDtype(tensorTy.getElementType())`.

    **Branch B — `else if (auto memDescTy = dyn_cast<MemDescType>(type))`:** New shared-memory path:
    - Shape: `memDescTy.getShape()` (assign to `input.shape`)
    - Encoding: `memDescTy.getEncoding()`
    - LinearLayout: `auto ll = mlir::triton::gpu::toLinearLayout(memDescTy);` — use the `MemDescType` overload (`LinearLayoutConversions.cpp:1376`), NOT `toLinearLayout(shape, encoding)`, because the MemDescType overload correctly calls `getAllocShape().take_back(getRank())` to handle subviews
    - Memory space: `input.memorySpace = "shared";`
    - Offset bases: `input.offsetBases = flattenBases(ll.getBases().lookup(kOffset));`
    - Block bases: `input.blockBases = flattenBases(ll.getBases().lookup(kBlock));`
    - Alignment: `auto sharedEnc = cast<SharedEncodingTrait>(encoding); input.alignment = sharedEnc.getAlignment();` — uses the MLIR interface (`TritonGPUAttrInterfaces.td:34–35`, default 16, overridden per encoding)
    - Dtype: `input.dtype = mapDtype(memDescTy.getElementType());`
    - Push `input` into `spec.inputs`.

    **flattenBases lambda:** Move the existing `flattenBases` lambda (currently lines 1467–1472) to before both branches (inside the walk lambda, after `mapDtype`) so both branches can call it. The lambda takes `auto bases` and flattens rows of a 2D structure to `SmallVector<int32_t, 16>`.

    Per RESEARCH.md Pitfall 3: shared layouts have ONLY `"offset"` and `"block"` input dims — NEVER use `kRegister`/`kLane`/`kWarp` in the shared branch.

    Per RESEARCH.md Pitfall 2: always use the `toLinearLayout(MemDescType)` overload, not `toLinearLayout(getShape(), getEncoding())`, for correct subview handling.
  </action>
  <verify>
    <automated>grep -n 'cast<RankedTensorType>\|dyn_cast<MemDescType>\|SharedEncodingTrait\|kOffset\|kBlock\|mapDtype' python/src/clang_compiler.cc</automated>
  </verify>
  <acceptance_criteria>
    - No `cast<RankedTensorType>(operand.getType())` remains in extractExternCallSpecs — `grep` returns 0
    - `dyn_cast<RankedTensorType>(type)` exists in a branch handling the distributed path
    - `dyn_cast<MemDescType>(type)` exists in a branch handling the shared path
    - Shared branch calls `toLinearLayout(memDescTy)` — NOT `toLinearLayout(shape, encoding)`
    - Shared branch calls `flattenBases(ll.getBases().lookup(kOffset))` and `.lookup(kBlock)`
    - Shared branch calls `cast<SharedEncodingTrait>(encoding).getAlignment()` for alignment
    - `mapDtype` lambda is defined once (outside the per-operand loop) and used by both branches
    - `flattenBases` lambda is defined once (outside the per-operand loop) and used by both branches
    - Pushes `TensorSpecInput` for tensor branch, `SharedSpecInput` for shared branch into `spec.inputs`
  </acceptance_criteria>
  <done>extractExternCallSpecs() uses dyn_cast branching: tensor path unchanged, shared path extracts offset_bases/block_bases/alignment via toLinearLayout(MemDescType).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update tritonExtractExternCallSpecs() JSON with std::visit</name>
  <files>python/src/clang_compiler.cc</files>
  <read_first>
    - python/src/clang_compiler.cc (lines 1509–1567, the tritonExtractExternCallSpecs function including the per-input JSON loop)
    - python/src/clang_compiler.h (line 36, confirms <variant> + <type_traits> for std::is_same_v)
    - third_party/nvidia/backend/compiler.py (lines 786–795, Phase 6 consumer uses .get() with defaults — must not crash on new keys)
  </read_first>
  <behavior>
    - Test 1: `grep -c 'std::visit' python/src/clang_compiler.cc` returns 1 (variant dispatch in JSON serialization)
    - Test 2: `grep -c 'if constexpr' python/src/clang_compiler.cc` returns 1 (compile-time type check in visit lambda)
    - Test 3: `grep -c '"memory_space"' python/src/clang_compiler.cc` returns 1 (new JSON key)
    - Test 4: `grep -c '"offset_bases"' python/src/clang_compiler.cc` returns 1
    - Test 5: `grep -c '"block_bases"' python/src/clang_compiler.cc` returns 1
    - Test 6: `grep -c '"alignment"' python/src/clang_compiler.cc` returns 1
    - Test 7: `grep -c '"num_warps"' python/src/clang_compiler.cc` returns 1 (tensor path field, unchanged)
    - Test 8: `grep -c '"reg_bases"' python/src/clang_compiler.cc` returns 1 (tensor path field, unchanged)
  </behavior>
  <action>
    In `tritonExtractExternCallSpecs()` (starts at line 1514), replace the per-input JSON serialization loop at lines 1531–1561 with a `std::visit` variant dispatch.

    The outer loop iterates `for (auto &inputV : spec.inputs)`. Replace the inner loop body with:

    ```
    std::visit([&](auto &input) {
      // common fields (dtype, shape) — same for both variants
      os << "{";
      os << "\"dtype\": \"" << input.dtype << "\", ";
      os << "\"shape\": [";
      for (size_t i = 0; i < input.shape.size(); ++i) {
        if (i > 0) os << ", ";
        os << input.shape[i];
      }
      os << "]";

      if constexpr (std::is_same_v<std::decay_t<decltype(input)>, TensorSpecInput>) {
        // Tensor variant: emit distributed-layout fields (unchanged from current code)
        os << ", \"num_warps\": " << input.numWarps;
        auto flatten = [&](auto &bases) {
          os << "[";
          for (size_t i = 0; i < bases.size(); ++i) {
            if (i > 0) os << ", ";
            os << bases[i];
          }
          os << "]";
        };
        os << ", \"reg_bases\": ";
        flatten(input.regBases);
        os << ", \"lane_bases\": ";
        flatten(input.laneBases);
        os << ", \"warp_bases\": ";
        flatten(input.warpBases);
      } else {
        // Shared variant: emit shared-memory layout fields
        os << ", \"memory_space\": \"" << input.memorySpace << "\"";
        auto flatten = [&](auto &bases) {
          os << "[";
          for (size_t i = 0; i < bases.size(); ++i) {
            if (i > 0) os << ", ";
            os << bases[i];
          }
          os << "]";
        };
        os << ", \"offset_bases\": ";
        flatten(input.offsetBases);
        os << ", \"block_bases\": ";
        flatten(input.blockBases);
        os << ", \"alignment\": " << input.alignment;
      }
      os << "}";
    }, inputV);
    ```

    **JSON discriminator strategy:** The shared variant emits `"memory_space": "shared"` (absent from tensor variant output). Phase 6 consumer at `compiler.py:786–795` can check `inp.get("memory_space")` to decide whether to build a `llvm.TensorParameter` or `llvm.SharedTensorParameter`. The tensor variant emits the same keys as before (`num_warps`, `reg_bases`, `lane_bases`, `warp_bases`) — backward-compatible.

    **Phase 5 consumer safety:** The existing Phase 5 consumer at `compiler.py:786–795` uses `.get()` with defaults (`inp.get("num_warps", 1)`, etc.) — it ignores unknown keys. New shared-input keys (`memory_space`, `offset_bases`, `block_bases`, `alignment`) will not cause a crash. No changes to `compiler.py` in this phase.

    The `std::visit` and `if constexpr` pattern is exhaustive — if a third variant is added later, the compiler will emit a warning (or error with `-Werror`). No `std::holds_alternative` or runtime branching needed.

    Keep the surrounding code unchanged: `os << "["` / `os << "]` delimiters, `firstSpec` / `firstInput` flags, spec-level fields (`symbol`, `libpath`, `use_fast_math`).
  </action>
  <verify>
    <automated>grep -n 'std::visit\|if constexpr\|"memory_space"\|"offset_bases"\|"block_bases"\|"alignment"' python/src/clang_compiler.cc</automated>
  </verify>
  <acceptance_criteria>
    - `std::visit` dispatches the variant in the per-input JSON loop — grep confirms exactly 1 occurrence
    - `if constexpr (std::is_same_v<std::decay_t<decltype(input)>, TensorSpecInput>)` appears in the visit lambda — grep confirms
    - JSON for tensor branch emits: `dtype`, `shape`, `num_warps`, `reg_bases`, `lane_bases`, `warp_bases` — same keys as before
    - JSON for shared branch emits: `dtype`, `shape`, `memory_space` (value `"shared"`), `offset_bases`, `block_bases`, `alignment` — grep confirms each key exactly once
    - No `"memory_space"` in the tensor branch output (only in `else` block)
    - No `num_warps` or `reg_bases` in the shared branch output (only in `if constexpr` tensor block)
  </acceptance_criteria>
  <done>tritonExtractExternCallSpecs() uses std::visit for variant-safe JSON — tensor output unchanged, shared output adds memory_space/offset_bases/block_bases/alignment.</done>
</task>

<task type="auto">
  <name>Task 4: Build libtriton.so + verify compilation + tensor regression check</name>
  <files>python/src/clang_compiler.cc</files>
  <read_first>
    - AGENTS.md (build: bash build.sh, CC=clang CXX=clang++, self-compiled LLVM path, clang_compiler.cc -fno-rtti)
    - CMakeLists.txt (clang_compiler.cc compile flags)
  </read_first>
  <action>
    Build the modified `clang_compiler.cc` using the canonical build command: `bash build.sh`. This runs cmake + ninja (with lld if available). The `clang_compiler.cc` file is compiled with `-fno-rtti` (permanent setting in CMakeLists.txt) — all Clang libs are built without RTTI.

    After build succeeds, copy the shared library: `cp build/libtriton.so python/triton/_C/libtriton.so`.

    **Regression verification:** Run the following Python snippet to verify tensor-only extraction produces identical JSON to pre-change:
    ```python
    import sys
    sys.path.insert(0, 'python')
    sys.path.insert(0, 'third_party/nvidia')
    from triton._C import llvm as triton_llvm
    
    # The tritonExtractExternCallSpecs function should be importable
    assert hasattr(triton_llvm, 'tritonExtractExternCallSpecs'), \
        "tritonExtractExternCallSpecs not found — build or binding issue"
    print("BUILD OK: tritonExtractExternCallSpecs is accessible")
    ```

    For a deeper regression check, run the E2E test suite (only tensor-path tests — shared-memory tests are Phase 7):
    ```
    PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" pytest python/test/gluon/test_extern_call.py -x -v
    ```
    All 6 existing tests must pass. These exercise `extractExternCallSpecs()` + `tritonExtractExternCallSpecs()` through the tensor-only path — confirming the variant rewrite and `dyn_cast` change don't break existing functionality.

    If any extern_call test fails, inspect the failure: the `std::visit` must be emitting exactly the same JSON for tensor inputs as before (same keys, same structure, same values).
  </action>
  <verify>
    <automated>bash build.sh && cp build/libtriton.so python/triton/_C/libtriton.so && PYTHONPATH="$(pwd)/python:$(pwd)/third_party/nvidia" python -c "from triton._C import llvm; assert hasattr(llvm, 'tritonExtractExternCallSpecs'), 'MISSING'; print('BUILD OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `bash build.sh` exits 0 (no compile errors in clang_compiler.cc or any dependency)
    - `tritonExtractExternCallSpecs` is importable from `triton._C.llvm` — Python import succeeds
    - All 6 existing `test_extern_call.py` tests pass — tensor-only extraction path regression-free
    - No crash, no segfault, no JSON parse error in the tensor-only code path
  </acceptance_criteria>
  <done>Build succeeds, tensor-only extraction regression-free — SHMLIR-02 satisfied (no crash on MemDescType path, shared-layout JSON emission ready for Phase 6 consumption).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLIR module → extractExternCallSpecs() | Untrusted operand types cross into C++: MemDescType must not trigger crash |
| extractExternCallSpecs() → tritonExtractExternCallSpecs() | Internal C++ boundary: variant types must be serialized correctly |
| JSON output → Python consumer (Phase 6) | String JSON crosses language boundary: keys must match consumer expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Denial of Service | extractExternCallSpecs() cast<RankedTensorType> | medium | mitigate | Replaced with dyn_cast on RankedTensorType + MemDescType — no crash on unexpected type. The MemDescType branch uses toLinearLayout(MemDescType) which already handles all 4 shared encoding variants. |
| T-05-04 | Tampering | JSON discriminator ambiguity | low | mitigate | Tensor variant emits no memory_space key; shared variant emits "memory_space": "shared". Phase 6 consumer distinguishes via presence check. No runtime branching ambiguity. |
| T-05-05 | Information Disclosure | JSON debug output | low | accept | JSON includes layout basis vectors and alignment — this is compiler-internal layout data, not user secrets. Only emitted during compilation, not persisted. |
| T-05-06 | Elevation of Privilege | std::visit exhaustion | low | accept | std::visit is compile-time exhaustive — if a third variant is added without updating this visitor, the compiler emits an error. No runtime fallthrough possible. |
| T-05-SC | Tampering | build dependencies | low | accept | No external packages installed. Internal C++ change only — uses existing LLVM/MLIR/clang infrastructure. |
</threat_model>

<verification>
  - `bash build.sh` succeeds (C++ compile)
  - `tritonExtractExternCallSpecs` is importable from Python
  - All 6 existing `test_extern_call.py` tests pass (tensor regression)
  - `grep 'cast<RankedTensorType>' python/src/clang_compiler.cc` returns 0 in extractExternCallSpecs range
  - `grep 'dyn_cast<MemDescType>' python/src/clang_compiler.cc` returns 1
  - `grep '"memory_space"' python/src/clang_compiler.cc` returns 1
</verification>

<success_criteria>
  - extractExternCallSpecs() uses dyn_cast branching — no blind cast<RankedTensorType> crash path
  - Shared-layout JSON includes memory_space/offset_bases/block_bases/alignment matching the MLIR SharedLinearEncodingAttr
  - Tensor-layout JSON is bitwise identical to pre-change output
  - All 6 existing extern-call tests pass unchanged
</success_criteria>

<output>
Create `.planning/phases/05-mlir-op-relaxation-spec-extraction/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | Location | Purpose |
|--------|------|----------|---------|
| `struct TensorSpecInput` | C++ struct | `python/src/clang_compiler.cc:1419` | Carries distributed-layout spec data (shape, numWarps, regBases, laneBases, warpBases) — replaces old `SpecInput` |
| `struct SharedSpecInput` | C++ struct | `python/src/clang_compiler.cc:1428` | Carries shared-layout spec data (shape, memorySpace, offsetBases, blockBases, alignment) — new for D-10 |
| `std::variant<TensorSpecInput, SharedSpecInput>` in `ExternCallSpec::inputs` | C++ variant type | `python/src/clang_compiler.cc:1435` | Type-safe discriminated union for per-operand spec data (D-10) |
| `StringAttr kOffset` | MLIR StringAttr | `python/src/clang_compiler.cc:1449` | Dim name "offset" for shared layout basis extraction |
| `StringAttr kBlock` | MLIR StringAttr | `python/src/clang_compiler.cc:1450` | Dim name "block" for shared layout basis extraction |
| `dyn_cast<MemDescType>` branch in `extractExternCallSpecs()` | C++ control flow | `python/src/clang_compiler.cc:1466` | Handles MemDescType operands without crash — replaces blind cast<RankedTensorType> |
| `toLinearLayout(MemDescType)` call in shared branch | C++ function call | `python/src/clang_compiler.cc:1470` | Converts MemDescType to LinearLayout for basis extraction — uses allocShape-correct overload |
| `cast<SharedEncodingTrait>(encoding).getAlignment()` | C++ interface call | `python/src/clang_compiler.cc:1482` | Extracts alignment from any shared encoding via MLIR interface |
| `std::visit` with `if constexpr` in `tritonExtractExternCallSpecs()` | C++ variant dispatch | `python/src/clang_compiler.cc:1535` | Type-safe JSON serialization dispatcher emitting variant-specific JSON keys |
| JSON key `"memory_space": "shared"` | JSON field | `python/src/clang_compiler.cc:1545` | Phase 6 discriminator: distinguishes shared from tensor inputs in JSON output |
| JSON keys `"offset_bases"`, `"block_bases"`, `"alignment"` | JSON fields | `python/src/clang_compiler.cc:1550–1553` | Shared-layout data emitted in extraction JSON for Phase 6 consumption |
